name: 🚦 CI

permissions:
  contents: read

on:
  push:
    branches: # Add any other mainline branch
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    name: 🧪 Test on ${{ matrix.os }} / Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.14"]
        os:
          - ubuntu-latest
          # - macos-latest
          # - windows-latest
    timeout-minutes: 15

    steps:
      - name: 🛎️ Checkout
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: 🐍 Install uv
        uses: astral-sh/setup-uv@d9e0f98d3fc6adb07d1e3d37f3043649ddad06a1 # v6.5.0
        with:
          python-version: ${{ matrix.python-version }}
          activate-environment: true
          enable-cache: true

      - name: 📥 Install dependencies
        run: uv sync --all-extras --dev

      # https://github.com/thombashi/pytest-md-report
      - name: 🧪 Run tests
        env:
          REPORT_OUTPUT: pytest_report.md
        run: |
          echo "REPORT_FILE=${REPORT_OUTPUT}" >> "$GITHUB_ENV"
          pytest -m "" --md-report --md-report-flavor=gfm --md-report-output="$REPORT_OUTPUT" --junitxml=junit.xml -o junit_family=legacy

      # TODO: Check why most of the uploads don't appear in the results
      # TRACK: https://github.com/codecov/test-results-action/issues/126
      - name: 📊 Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/test-results-action@47f89e9acb64b76debcd5ea40642d25a4adced9f # v1.1.1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

      - name: 📊 Add test output reports to the job summary
        if: failure()
        run: |
          if [ -f "$REPORT_FILE" ]; then
            {
              echo "<details><summary>Test Report</summary>"
              echo ""
              cat "$REPORT_FILE"
              echo ""
              echo "</details>"
            } >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: 📊 Upload coverage to Codecov
        uses: codecov/codecov-action@39a2af19d997be74586469d4062e173ecae614f6 # 5.4.3
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  lint:
    name: 🧹 Lint & Type Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: 🛎️ Checkout
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: 🐍 Install uv
        uses: astral-sh/setup-uv@d9e0f98d3fc6adb07d1e3d37f3043649ddad06a1 # v6.5.0
        with:
          python-version: 3.12
          activate-environment: true
          enable-cache: true
      - name: 📥 Install dependencies
        run: uv sync --all-extras --dev

      - name: 🔍 Lint check
        uses: astral-sh/ruff-action@fbc7e4525b98c5a6c8104fbd7bba4f06eab4f853 # 3.4.0
        with:
          args: check --output-format github

      - name: 🧹 Format check
        run: ruff format --check --diff

      - name: 🔍 Type check
        run: basedpyright
